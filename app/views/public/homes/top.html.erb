<div class="container-fluid">
  <div class="row justify-content-center">
    <div class="col-12 col-xl-10 px-0">
      <div class="main-visual">
        <div id="main-visual-swiper" class="swiper">
          <div class="swiper-wrapper">
            <% @rand_items.each do |item| %>
              <div class="swiper-slide">
                <div class="swiper-slide-bg" style="background-image: url(<%= rails_blob_path(item.image) %>)" data-swiper-parallax-x="90%"></div>
                <div class="swiper-slide-inner">
                  <p class="swiper-slide-heading"><%= item.name %></p>
                  <p class="swiper-slide-p"><%= item.introduction %></p>
                  <p class="swiper-slide-p">¥<%= item.price %></p>
                  <div class="swiper-slide-link" data-swiper-parallax-x="100%">
                    <%= link_to '商品ページへ', item_path(item.id), class: 'btn btn-danger btn-sm' %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        </div>

        <div class="main-visual__text">
          <div class="swiper-pagination"></div>
          <p class="main-visual__text-heading">ようこそ、<br>ながのCAKEへ！</p>
          <p class="main-visual__text-p">このサイトは、ケーキ販売のECサイトになります。</p>
          <p class="main-visual__text-p">会員でない方も商品の閲覧はできますが、<br>購入には会員登録が必要です。
        </div>
      </div>
    </div>
  </div>
</div>

<div class="container">
  <div class="row justify-content-between">
    <div class="col-md-3">
      <%= render 'public/genres/search-box' %>
    </div>
    <div class="col-md-8">
      <div class="row">
        <div class="col-12">
          <h2>新着商品</h2>
        </div>
      </div>
      
      <div class="row">
        <div id="new-items-swiper" class="swiper">
          <div class="swiper-wrapper">
            <% @new_items.each do |item| %>
              <div class="swiper-slide">
                <div class="row">
                  <%= image_tag item.image, size: "150x100" %>
                </div>
                <%= link_to item.name, item_path(item.id), class: 'swiper-slide-link' %>
                <p class="swiper-slide-p"><%= item.introduction %></p>
                <p class="swiper-slide-p">¥<%= item.price %></p>
              </div>
            <% end %>
          </div>
        </div>
      </div>

      <div class="row justify-content-end">
        <%= link_to '全ての商品を見る >', items_path %>
      </div>
    </div>
  </div>
</div>